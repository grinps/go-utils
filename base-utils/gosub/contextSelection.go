package gosub

/*
func WithContext(onSelect OnSelect, contextValue context.Context) Selectable {
	return SelectorFunction(func(collection SelectCollection) error {
		returnError := collection.Register(contextValue, onSelect, NewProxyChannel(contextValue.Done(), make(chan interface{})).Begin())
		return returnError
	})
}

*/
